server:
  port: 8083

spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1523/xe
    username: system
    password: Oracle123
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

